import React from "react";

const ChatIsNotSelected = () => {
	return (
		<div className="h-full w-full text-bold text-green-800 flex justify-center items-center">
			<h1>You can select a COUNTRY chat now! </h1>
		</div>
	);
};

export default ChatIsNotSelected;
